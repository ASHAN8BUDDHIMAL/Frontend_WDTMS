import React from "react";

const Settings = () => {
  return (
    <div style={styles.container}>
      <h1>Settings</h1>
      <p>Admin settings and configurations page.</p>
    </div>
  );
};

const styles = {
  container: {
    padding: "20px"
  }
};

export default Settings;
